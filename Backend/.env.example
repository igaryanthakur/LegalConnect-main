# ===========================================
# Add ALL of these to Vercel: Project Settings → Environment Variables
# .env is NOT deployed - variables must be set in Vercel Dashboard
# ===========================================

# MongoDB (required)
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/legalconnect?retryWrites=true&w=majority

# JWT (required)
JWT_SECRET=your_secure_random_jwt_secret
JWT_EXPIRE=7d

# Auth.js (required)
AUTH_SECRET=your_secure_auth_secret
AUTH_URL=https://your-backend.vercel.app

# Optional
GEMINI_API_KEY=
USE_LOCAL_STORAGE=false
IMAGEKIT_PUBLIC_KEY=
IMAGEKIT_PRIVATE_KEY=
IMAGEKIT_URL_ENDPOINT=
